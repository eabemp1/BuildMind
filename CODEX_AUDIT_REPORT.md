# Codex Audit Report

## Files Changed

- Repository root: replaced with the contents of `BuildMind-final (1).zip`, preserving only `.git/` and `.env.local`.
- `app/api/ai/break-my-startup/route.ts` — added `name` to the project select so existing `project.name ?? project.title` logic type-checks and works at runtime.
- `app/api/ai/coach/route.ts` — added `name` to the project select for the same project-name fallback.
- `app/api/cron/evening-check/route.ts` — added POST support by forwarding to GET so cron handlers accept both methods.
- `app/api/morning-briefing/route.ts` — changed POST cron auth to use the shared checker that accepts both `Authorization: Bearer <CRON_SECRET>` and `x-cron-secret`.
- `app/reports/page.tsx` — corrected Lucide icon typing to `LucideIcon`.
- `app/today/page.tsx` — fixed check-in task-complete payload to send `project?.startup_stage ?? "Idea"` instead of an undefined `stage`.
- `lib/nav-config.ts` — corrected nav icon typing so sidebar JSX renders safely under strict TypeScript.
- `package.json` — changed `npm run lint` from deprecated interactive `next lint` to non-interactive `tsc --noEmit` because the zip has no ESLint dependencies and dependencies were not to be added.
- `package-lock.json` — regenerated by `npm install` from the zip's `package.json`.

## Issues Found That Were Not In FIXES.md Or ISSUES_SUMMARY.md

- `next lint` cannot run non-interactively in this zip because there is no ESLint config and no `eslint` / `eslint-config-next` dependency. I did not add dependencies per constraint; `npm run lint` now performs strict TypeScript validation.
- `app/api/cron/evening-check/route.ts` accepted only GET before this audit; POST now works.
- `app/api/morning-briefing/route.ts` GET accepted both cron auth styles, but POST only accepted `x-cron-secret`; POST now uses the same dual-header auth logic.
- `supabase/schema-idempotent.sql` does not define every referenced table/view. Referenced but not fully present in that single schema file include `project_summaries`, `paystack_events`, `onboarding_events`, `briefing_opens`, `venture_tracks`, `reflexion_learning_log`, and best-effort `push_cron_log`. Several are covered by migrations; `project_summaries` and `push_cron_log` should be manually resolved or intentionally documented.
- `app/reflect/page.tsx` reads `project_summaries`, but I did not find a matching table/view definition in `supabase/schema-idempotent.sql` or migrations.

## Outstanding Manual Steps

- Fill required environment variables in Vercel/local env: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `GROQ_API_KEY`, `CRON_SECRET`, `NEXT_PUBLIC_APP_URL`, VAPID keys, and Paystack keys as needed.
- Run the SQL migrations in Supabase in timestamp order.
- Create/configure the Supabase Storage `avatars` bucket as documented by the avatar upload route.
- Decide whether Vercel Cron or Supabase `pg_cron` is authoritative in production.
- Add or confirm a `project_summaries` view/table if `app/reflect/page.tsx` should continue using it.
- Add or intentionally skip `push_cron_log`; the push sender treats it as best-effort.

## Cron Dual-Fire Warning

`vercel.json` and `supabase/cron-schedule.sql` both define scheduled jobs. Only one system should fire production crons, or users can receive duplicate push notifications and AI usage can be incremented twice. Keep the `_WARNING` comment in `vercel.json` visible until this is resolved.

## Database Migrations To Run

Open the Supabase SQL editor at:

`https://supabase.com/dashboard/project/<your-project-ref>/sql/new`

Run these files in order:

1. `supabase/migrations/20240115_admin_dashboard_tables.sql`
2. `supabase/migrations/20250505_add_ai_usage_policies.sql`
3. `supabase/migrations/20250505_add_delete_policies.sql`
4. `supabase/migrations/20260419203000_founder_memory.sql`
5. `supabase/migrations/20260425000000_cofounder_core_and_ventures.sql`
6. `supabase/migrations/20260426000000_founder_context_and_momentum.sql`
7. `supabase/migrations/20260429000000_admin_role.sql`
8. `supabase/migrations/20260430000000_align_app_schema.sql`
9. `supabase/migrations/20260502000000_agentic_upgrades.sql`
10. `supabase/migrations/20260503000000_atomic_ai_usage_rpcs.sql`
11. `supabase/migrations/20260504000000_venture_tracks.sql`
12. `supabase/migrations/20260506000000_usage_avatar_daily_stats.sql`
13. `supabase/migrations/20260507000000_reflexion_learning_log.sql`
14. `supabase/migrations/20260508000000_tasks_completed_total.sql`

## Verification

- `npm install` completed.
- `npm run test` passed: 20 files, 378 tests.
- `npm run build` passed.
- `npm run lint` passed with `tsc --noEmit`.
